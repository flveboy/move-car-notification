{
  "name": "car-move-service",
  "version": "1.0.0",
  "scripts": {
    "build": "echo 'No build step needed'",
    "dev": "wrangler pages dev --local --persist"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
